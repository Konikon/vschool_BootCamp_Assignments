ipChecker = str =>{
    const array = str.split(".");
    if(str.length < 4){
        return false;
    }
//   check for dots in right place
    
//    check for numbers only no text/symbols
    
    if(str.filter(str => str === NaN){
       return false;
       }) 
    
//    check for -ve numbers
    
    
    if(str.filter(str => str < 0))return false;
};

module.exports = ipChecker;
    
    
